<UserControl x:Class="ExperimentalProject.Views.WidgetPalette"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExperimentalProject"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:WidgetPalette}" Height="100" Width="100" Margin="5pt">
    <Grid>
        <Button Background="SteelBlue" Command="{Binding CreateWidgetCommand}" ></Button>
        <Grid Height="20pt" Background="#7F000000" VerticalAlignment="Bottom">
            <TextBlock VerticalAlignment="Center" Foreground="Azure" FontSize="10pt" HorizontalAlignment="Center" Text="{Binding Title}"/>
        </Grid>
    </Grid>
</UserControl>
